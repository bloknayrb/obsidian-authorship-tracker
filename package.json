{
  "name": "authorship-tracker",
  "version": "1.0.1",
  "description": "Tracks who creates and modifies notes — designed for human/AI collaborative vaults.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck -p tsconfig.json && node esbuild.config.mjs production",
    "lint": "eslint .",
    "typecheck": "tsc -noEmit -skipLibCheck -p tsconfig.json",
    "typecheck:test": "tsc -noEmit -skipLibCheck -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "verify": "npm run lint && npm run typecheck && npm run typecheck:test && npm test"
  },
  "keywords": [
    "obsidian",
    "authorship",
    "provenance",
    "metadata"
  ],
  "author": "Bryan Kolb",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "builtin-modules": "3.3.0",
    "esbuild": "0.25.0",
    "eslint": "^8.57.0",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "4.7.4",
    "vitest": "^3.2.6"
  }
}
