# Obsidian requires the release tag to match manifest.json's version exactly,
# with no prefix. npm's default would tag v1.0.2 while version-bump.mjs writes
# 1.0.2, producing a release the plugin updater cannot map.
tag-version-prefix=""
